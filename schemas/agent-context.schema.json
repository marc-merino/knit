{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Knit agent context",
  "type": "object",
  "required": ["schemaVersion", "kind", "id", "activeBundle", "updatedAt"],
  "properties": {
    "schemaVersion": { "const": "0.1" },
    "kind": { "const": "KnitAgentContext" },
    "id": { "type": "string" },
    "activeBundle": { "type": "string" },
    "updatedAt": { "type": "string" }
  }
}
